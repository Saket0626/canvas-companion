{
  "name": "canvas-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that turns Canvas courses into one calendar with a scoped assistant.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.4",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  }
}
