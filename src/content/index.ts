/**
 * Content script — Phase 0 stub.
 *
 * Phase 3 will:
 *   (a) detect the current course ID from the URL/DOM and message it
 *   (b) inject a small floating launcher that opens the side panel
 *
 * This script must NEVER fetch Canvas data or hold assignment state.
 * Multiple Canvas tabs can inject this file; all network stays in the worker.
 */

const PHASE = 0;

console.info(`[Canvas Companion] content script loaded (phase ${PHASE})`);
